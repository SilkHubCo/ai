---
'@tanstack/ai-anthropic': patch
---

Preserve input and prompt-cache usage from Anthropic `message_start` events, include cache tokens in normalized prompt totals, and report known usage with terminal stream errors.
